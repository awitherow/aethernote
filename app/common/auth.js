export function authenticateLogin({ userId, userKey }) {
  const validUserId = "";
  const validUserKey = "";
  if (userId === validUserId && userKey === validUserKey) {
    return true;
  } else {
    return false;
  }
}
